const chapterType = {
    LECTURE: 0,
    VIDEO: 1,
    QUIZ: 2
}

const questionType = {
    MCQ: 'MCQ', // multiple choice question,
    MAQ: 'MAQ', // multiple answer question,
    BOOLEAN: 'boolean', // true/false question,
    BOOLEAN1: 'boolean', // true/false question,
    BOOLEAN2: 'boolean',
    BOOLEAN3: 'boolean',
}

module.exports = { chapterType, questionType }